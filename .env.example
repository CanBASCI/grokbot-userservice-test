# Copy to .env and replace placeholders. Never commit .env.
# No real secrets belong in git or chat.

# Host publish for api-gateway only (container listen stays 8080)
HTTP_PORT=8080

# Scale (Compose --scale). Gateway must stay 1 without an LB on :8080.
REPLICAS_GATEWAY=1
REPLICAS_SIGNUP_SERVICE=1
REPLICAS_LOGIN_SERVICE=1

SIGNUP_DB_USER=signup
SIGNUP_DB_PASSWORD=change-me-signup-db

LOGIN_DB_USER=login
LOGIN_DB_PASSWORD=change-me-login-db

# Must be at least 32 characters (login-service boot fails otherwise).
JWT_SECRET=change-me-use-at-least-32-chars!!

JWT_ACCESS_TTL_SECONDS=900
REFRESH_TTL_SECONDS=2592000

# Private compose network gRPC (both services listen 9090; DNS names differ)
SIGNUP_GRPC_TARGET=static://signup-service:9090
LOGIN_GRPC_TARGET=static://login-service:9090
